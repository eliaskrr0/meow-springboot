services:
  micro-manager-food:
    image: micro-manager-food:latest
    build:
      context: ..
      dockerfile: micro-manager-food/Dockerfile
    environment:
      - DB_HOST=${DB_DOCKER_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - SPRING_PROFILES_ACTIVE=local
    ports:
      - "8080:8080"
    restart: unless-stopped
    networks:
      - meow-network

networks:
  meow-network:
    external: true
